# راه‌اندازی این مستندات

این صفحه توضیح می‌ده که خود این سایت مستندات چطور ساخته شده،
تا در آینده بشه به همین روش صفحات جدید اضافه کرد یا سیستم رو نگه‌داری کرد.

## ابزارهای استفاده‌شده

- **MkDocs** — جنریتور سایت مستندات از فایل‌های Markdown
- **Material for MkDocs** — تم ظاهری سایت
- **GitHub Pages** — میزبانی رایگان سایت
- **GitHub Actions** — دیپلوی خودکار

## ساختار پروژه

\`\`\`
docs/
├── docs/              # فایل‌های Markdown محتوا
│   ├── index.md
│   └── setup.md
├── mkdocs.yml         # فایل تنظیمات
└── .github/
    └── workflows/
        └── ci.yml     # پایپ‌لاین دیپلوی خودکار
\`\`\`

## نحوه‌ی افزودن صفحه جدید

1. یه فایل `.md` جدید داخل پوشه `docs/` بساز
2. اون رو به بخش `nav` در `mkdocs.yml` اضافه کن
3. کامیت و پوش کن به شاخه `main`
4. GitHub Actions خودکار سایت رو آپدیت می‌کنه

## تست محلی قبل از پوش کردن

\`\`\`bash
mkdocs serve
\`\`\`

و برو به `http://127.0.0.1:8000`
